const config = {
    port: 3000,
    database: {
        DATABASE: 'koa_demo',
        USERNAME: 'root',
        PASSWORD: '111111',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;